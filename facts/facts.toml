# sondir facts database — the knowledge no Cargo metadata or RPC schema expresses.
#
# Every entry carries evidence (canary id / date) so it can be re-verified or
# retired. Override at runtime with `sondir --facts <path>` — this file ships
# embedded in the binary as the default. Destined for its own community repo
# (rustsec-style) once sondir goes public.

[[gates]]
address = "5cC3foj77CWun58pC51ebHFUWavHWKarWyR5UUik7dnC"
simd = "SIMD-0178/0189/0377"
name = "SBPFv3 deployment and execution"
consequence = "arch v3 .so files are deployable/executable on this cluster; with this gate the e_flags direct mapping also accepts v1/v2 deploys (canary c15, devnet 2026-07-03)"

[[gates]]
address = "YbbRLkvenrocjGPGyoQE4wjnvYzTgfsk38NFmcYK7a5"
simd = "SIMD-0431"
name = "Loader-v3 minimum extend program size"
consequence = "ExtendProgram must add >= 10240 bytes (or extend to max); small auto-extends on upgrade FAIL after the buffer is written"

[[gates]]
address = "B8JJXCy5amZyWG9r7EnUYLwzXSXTxG7GZ1qZ1qggo83g"
simd = "SIMD-0500"
name = "Disable deployment of SBPF v0, v1 and v2 programs"
consequence = "only arch v3 .so files remain deployable"

[[conflicts]]
id = "litesvm-magicblock"
a = "litesvm 0.13.x"
b = "ephemeral-rollups-sdk (vrf) / ephemeral-vrf-sdk >=0.3"
why = "litesvm 0.13.x pins solana-instruction =3.2.0 (Agave 4.0 wave) while the MagicBlock chain requires ^3.4 (Agave 4.1 wave); cargo cannot unify same-major exact vs caret. RESOLVED UPSTREAM: litesvm 0.14.0 (2026-07-13) moved to solana-instruction ^3.4.0 / agave-* ^4.1.1 — exactly the Agave-4.1-wave release this entry was waiting on (facts-verify 2026-07-27). Kept, bounded to 0.13.x, because `anchor init` (CLI 1.1.2) templates still ship litesvm 0.13.1, so fresh-project + MagicBlock hits this immediately (canary c05)."
fix = "upgrade litesvm to >=0.14 (the Agave-4.1-wave release)"
probe = ["litesvm@>=0.13, <0.14", "ephemeral-rollups-sdk@*#anchor,vrf"]
fix_pin = "litesvm@0.14"

[[conflicts]]
id = "litesvm-instructions-sysvar"
a = "litesvm 0.13.x"
b = "solana-instructions-sysvar >=3.0.1"
why = "solana-instructions-sysvar 3.0.1 requires solana-instruction ^3.4.0; litesvm 0.13.x pins =3.2.0 (canary c06). RESOLVED UPSTREAM in litesvm 0.14.0 (^3.4.0, facts-verify 2026-07-27). If you must stay on 0.13.x, the escapes remain sysvar =3.0.0 (rides ^3.0.0) or litesvm 0.12.x."
fix = "upgrade litesvm to >=0.14, or pin solana-instructions-sysvar = \"=3.0.0\""
probe = ["litesvm@>=0.13, <0.14", "solana-instructions-sysvar@>=3.0.1"]
fix_pin = "litesvm@0.14"

[[conflicts]]
id = "legacy-solana-program"
a = "solana-program 1.x (legacy)"
b = "modern anchor workspace (solana-* 3.x wave)"
why = "the legacy 1.x line drags curve25519-dalek 3.2.1 whose zeroize pin clashes with the modern tree (canary c19: `failed to select a version for zeroize`). Remove the direct solana-program dep and use anchor_lang re-exports (or granular solana-* 3.x crates)."
fix = "remove the direct solana-program dependency; use anchor_lang::solana_program re-exports"
# A bare pair resolves — cargo backtracks the other crate to an ancient
# compatible release. The claim needs a MODERN member pinned; litesvm >=0.12
# represents the 3.x-wave workspace and reproduces c19's zeroize clash
# exactly (facts-verify calibration 2026-07-04).
probe = ["solana-program@>=1.14, <2", "litesvm@>=0.12"]

[[conflicts]]
id = "litesvm-light"
a = "litesvm 0.13.x"
b = "light-sdk >=0.24"
why = "light-sdk 0.24 sits on the Agave-4.1 interface wave (solana-instruction ^3.4) while litesvm 0.13.x pins =3.2.0 — same schism as litesvm-magicblock (sweep discovery 2026-07-04). RESOLVED UPSTREAM in litesvm 0.14.0 (facts-verify 2026-07-27)."
fix = "upgrade litesvm to >=0.14 (the Agave-4.1-wave release)"
probe = ["litesvm@>=0.13, <0.14", "light-sdk@>=0.24"]
fix_pin = "litesvm@0.14"

[[conflicts]]
id = "mollusk-magicblock"
a = "mollusk-svm 0.13.x"
b = "ephemeral-rollups-sdk (vrf) / ephemeral-vrf-sdk >=0.3"
why = "mollusk-svm 0.13.x had the SAME disease as litesvm 0.13.x: its agave-syscalls 4.0.0 pinned the Agave-4.0 interface wave, unresolvable with MagicBlock's ^3.4 requirements (sweep discovery 2026-07-04). RESOLVED UPSTREAM: mollusk-svm 0.14.0 (2026-07-08) drops agave-syscalls and rides agave-feature-set / solana-program-runtime ^4.1.1 (facts-verify 2026-07-27)."
fix = "upgrade mollusk-svm to >=0.14 (the Agave-4.1-wave release)"
probe = ["mollusk-svm@>=0.13, <0.14", "ephemeral-rollups-sdk@*#anchor,vrf"]
fix_pin = "mollusk-svm@0.14"

[[conflicts]]
id = "mollusk-light"
a = "mollusk-svm 0.13.x"
b = "light-sdk >=0.24"
why = "mollusk-svm 0.13.x (agave-syscalls 4.0.0, Agave-4.0 wave pins on solana-pubkey) could not resolve with light-sdk 0.24's 4.1-wave requirements (sweep discovery 2026-07-04). RESOLVED UPSTREAM in mollusk-svm 0.14.0 (facts-verify 2026-07-27)."
fix = "upgrade mollusk-svm to >=0.14 (the Agave-4.1-wave release)"
probe = ["mollusk-svm@>=0.13, <0.14", "light-sdk@>=0.24"]
fix_pin = "mollusk-svm@0.14"

[[conflicts]]
id = "mollusk-instructions-sysvar"
a = "mollusk-svm 0.13.x"
b = "solana-instructions-sysvar >=3.0.1"
why = "solana-instructions-sysvar 3.0.1+/4.x requires the Agave-4.1 interface wave; mollusk-svm 0.13.x pinned the 4.0 wave via agave-syscalls 4.0.0 (sweep discovery 2026-07-04). RESOLVED UPSTREAM in mollusk-svm 0.14.0 (facts-verify 2026-07-27). If you must stay on 0.13.x, pin sysvar =3.0.0."
fix = "upgrade mollusk-svm to >=0.14, or pin solana-instructions-sysvar = \"=3.0.0\""
probe = ["mollusk-svm@>=0.13, <0.14", "solana-instructions-sysvar@>=3.0.1"]
fix_pin = "mollusk-svm@0.14"

# Conflicts cargo's RESOLVER accepts and rustc then rejects. Verified by probing
# with `cargo check` (must resolve, then FAIL to compile) instead of
# `generate-lockfile` — a [[conflicts]] entry here would report "resolves" and be
# marked stale forever.

[[compile_conflicts]]
id = "wincode-schema-split"
a = "the wincode 0.6 wave: solana-clock >=3.2, solana-hash >=4.6, solana-address >=2.7, solana-message >=4.4, solana-epoch-rewards/-last-restart-slot/-short-vec/-slot-hashes/-slot-history >=3.2"
b = "the wincode 0.5 wave: solana-instruction 3.4.0 (still the newest release), plus solana-sysvar, solana-nonce, solana-transaction, litesvm"
why = "the solana-* interface crates are mid-migration between wincode 0.5 and 0.6. Seven have moved to ^0.6.0 while solana-instruction 3.4.0 — the LATEST, not a laggard — still derives its SchemaRead/SchemaWrite impls from ^0.5.0. With both majors in one graph, `Pubkey` fails the trait bound and rustc rejects it (E0277) in solana-sysvar and solana-nonce. Cargo resolves the graph happily, so this is invisible to every resolve-level probe: `sweep` reported 66/66 clean on 2026-07-27, the same day a bare `cargo add litesvm@0.15` could not be built (canary c22/c24). Existing projects survive only because their Cargo.lock predates the split — `cargo update` walks straight into it."
fix = "keep the 0.6-wave crates back (an intact Cargo.lock already does this; explicit upper bounds if you need it enforced) until solana-instruction ships against wincode 0.6. There is no single-crate pin: seven crates moved, and bounding them individually means seven bounds to remember to lift"
probe = ["litesvm@0.15"]
split_crate = "wincode"

[[litesvm_runtimes]]
prefix = "0.12."
arch_ok = [1, 2]
note = "litesvm 0.12 = Agave 3.1.14 runtime: test .so must be --arch v1 (or v2); v0 from platform-tools >=1.54 and v3 both fail"

[[litesvm_runtimes]]
prefix = "0.13."
arch_ok = [3]
note = "litesvm 0.13 = Agave 4.0 runtime (SBPF v3). NOTE: its solana-instruction =3.2.0 exact pin conflicts with the whole Agave-4.1 wave (MagicBlock vrf-sdk, light-sdk, instructions-sysvar >=3.0.1) — unresolvable in one graph; fixed upstream in litesvm 0.14.0 (2026-07-13)"

[[litesvm_runtimes]]
prefix = "0.14."
arch_ok = [1, 2, 3]
note = "litesvm 0.14 = Agave 4.1 runtime: executes v1, v2 AND v3 — the arch trap of 0.12/0.13 is gone, any build-sbf --arch runs (canary c21, 2026-07-27). Deployability is a separate question: SIMD-0500 still bars v0/v1/v2 from the cluster, so a test that passes here can still fail `anchor deploy`"

[[litesvm_runtimes]]
prefix = "0.15."
arch_ok = [1, 2, 3]
note = "litesvm 0.15 = Agave 4.1 runtime, same arch tolerance as 0.14: v1/v2/v3 all execute (canary c21, 2026-07-27)"
